/*
import { describe, it, expect } from 'vitest';
import ElementFactory from './ElementFactory';
import Line from './Line';
import Rectangle from './Rectangle';

describe('ElementFactory', () => {
  it('should create a Line element', () => {
    const element = ElementFactory.createElement('line', 0, 0, 100, 100);
    expect(element).toBeInstanceOf(Line);
    expect(element.draw).toBeInstanceOf(Function);
  });

  it('should create a Rectangle element', () => {
    const element = ElementFactory.createElement('rectangle', 0, 0, 100, 100);
    expect(element).toBeInstanceOf(Rectangle);
    expect(element.draw).toBeInstanceOf(Function);
  });
});
*/